<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nf.battlechip">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_battlechip_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_battlechip_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.BattleChip"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.LobbyActivity"/>
        <activity android:name=".activity.UserStatisticsActivity" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.MainUnityActivity" />
    </application>

</manifest>